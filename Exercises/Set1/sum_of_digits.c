/**
 * Define two function
 *
 * 1. last(n): which takes
 * a number and returns the last digit.
 *
 * Example: last(15) will retun 5, last(7) returns 7
 *
 * 2. rest(n): which takes a number and
 * returns the number by removing the last digit
 *
 * Example: rest(123456) return 12345, rest(43) returns 4,
 * rest(7) returns 0.
 *
 * Use these two functions to find the sum of digits of 7346
 *
 */