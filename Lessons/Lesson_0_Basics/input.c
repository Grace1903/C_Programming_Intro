/** A program which reads a number from the input
  * and print the same to the output.
  */

#include <stdio.h>

int main()
{
  int num;
   
  printf("enter the number: ");
  scanf("%d", &num);
  
  printf("num = %d\n", num);

  return 0;
}
